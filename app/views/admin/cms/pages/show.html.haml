%div.row
  = link_to edit_admin_cms_page_path(@page), class: 'button' do
    %i.fi-page-edit
    Edit

%div.row
  = render partial: "shared/page", object: @page

%hr

%div.row
  %h2 Change history
  = render partial: "shared/audits", object: @page.audits
